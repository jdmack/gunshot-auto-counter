<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen theme (dark variant) -->
    <style name="Theme.ShotCounterPoc.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#071E33</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenIconBackgroundColor">#071E33</item>
        <item name="postSplashScreenTheme">@style/Theme.ShotCounterPoc</item>
    </style>

</resources>
